{
  "name": "wanderlust-mern-platform",
  "version": "2.0.0",
  "description": "Enterprise-Grade Multi-Tenant Hospitality Platform & SaaS (MERN Architecture)",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm run start --prefix backend",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend"
  },
  "keywords": ["mern", "react", "express", "mongodb", "hospitality", "saas"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
